{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "outputs": []
    },
    "build": {
      "outputs": ["build", ".cache", "public/build"]
    },
    "deploy": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
