{
  "private": true,
  "sideEffects": false,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "turbo run deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^1.5.5"
  },
  "engines": {
    "node": ">=16"
  },
  "workspaces": [
    "workers/*",
    "apps/*"
  ]
}
